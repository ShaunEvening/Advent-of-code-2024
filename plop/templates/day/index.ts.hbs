import chalk from 'chalk';
import { Logger } from '@common/utils/console.utils';

Logger.printHeader({{ dayNumber }}, '{{ dayTitle }}');

console.log(`${chalk.bold.green('Day {{ dayNumber }}')} - part one:`, '\n');

console.log(`${chalk.bold.green('Day {{ dayNumber }}')} - part two:`, '\n');
